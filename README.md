# Guess the Number Telegram Bot

A small Telegram bot game where the user tries to guess a hidden number.

## Setup

1. Install dependencies:
   ```bash
   pip install pyTelegramBotAPI

2. Put your bot token in the config file:
    * open cfg/config.py
    * replace the placeholder token with your real bot token

3. Run the bot:
    python main.py